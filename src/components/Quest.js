import React, { useState } from "react";
{
  /*import { VRCanvas, DefaultXRControllers } from "@react-three/xr";
import { Canvas } from "@react-three/fiber";
import { Box, Text } from "@react-three/drei";
import { XR } from "@react-three/xr";

const questions = [
  "You think your friend is mad at you because they cancelled your plans together. Challenge the thought.",
  "What's a coping skill you can use when you're depressed?",
  "What does it feel like in your body when you're relaxed?",
];

const Card = ({ position, onClick }) => (
  <Box position={position} onClick={onClick}>
    <meshStandardMaterial attach="material" color="white" />
  </Box>
);

const Quest = () => {
  const [selectedQuestion, setSelectedQuestion] = useState(null);

  const handleCardClick = (index) => {
    setSelectedQuestion(questions[index]);
  };

  return (
    <VRCanvas>
      <ambientLight />
      <pointLight position={[10, 10, 10]} />

      
      <Box position={[0, 0, 0]} args={[10, 0.1, 10]}>
        <meshStandardMaterial attach="material" color="green" />
      </Box>

      
      {questions.map((_, index) => (
        <Card
          key={index}
          position={[index * 2 - 2, 0.5, 0]}
          onClick={() => handleCardClick(index)}
        />
      ))}

      
      {selectedQuestion && (
        <Text position={[0, 2, 0]} fontSize={0.5} color="black">
          {selectedQuestion}
        </Text>
      )}

      <DefaultXRControllers />
    </VRCanvas>
  );
};

export default Quest;*/
}
